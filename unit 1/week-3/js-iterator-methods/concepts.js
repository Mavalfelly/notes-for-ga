/*
********************
Array Iterator Methods
********************

what are iterator method

*/

const arr = []
arr.push('matt')// this is an example of an array method called push. it adds to an array
console.log(arr)

/*
Notice how we have a period followed by a push
this push adds to the array.

*/


/*
**************
Array Iterator Methods
**************
***What are array iterator methods***
these methods iterate (also called loop) through each value (also called element) in the array
and does an action along each interation (or loop)
so in other words they are specific set of array methods that loops through each value in the array
and there are 8 of them
forEach()
map()
filter()
some()
every()
find()
findIndex()
reduce()
*/